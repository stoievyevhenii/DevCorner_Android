<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.stoiev.devcorner">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ResultActivity"
            android:theme="@style/ResultPage"
             />
        <activity android:name=".DB.RoomActions" />
        <activity android:name=".AccountPageActivity"
            android:theme="@style/AccountPage"
            android:label="@string/menuAccount"/>
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/LoginPage" />
        <activity
            android:name=".ExercisePageActivity"
            android:theme="@style/ExercisePage" />
        <activity
            android:name=".NewExerciseActivity"
            android:label="@string/title_activity_new_exercise"
            android:theme="@style/NewExercisePage"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/HomePage" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/LoginPage" />
    </application>

</manifest>